<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/login-style.css" />
    <title>Document</title>
</head>
<body>
    <div class="background"></div>
    <div class="container">
        <h2>התחברות ספק</h2>
        <% if (typeof error !== 'undefined') { %>
            <p class="error"><%= error %></p>
          <% } %>
        <form action="/s/login" method="POST">
          <input type="text" name="companyName" placeholder="שם חברה" required />
          <input type="password" id="password" name="password" placeholder="סיסמה" required oninput="validatePasswordLength()" />
        <div class="password-toggle" onclick="togglePassword()">הצג/הסתר סיסמה</div>
        <div id="password-error" style="color: red; font-size: 13px; display: none;">הסיסמה חייבת להכיל בין 6 ל־8 תווים.</div>
          <button type="submit">התחבר</button>
        </form>
      </div>


      <script src="/scripts/login-script.js"></script>
</body>
</html>